#!/usr/bin/env ruby

require 'erb'

directoryDB = `echo $DBDIR`.strip
directoryBin = File.expand_path File.dirname(__FILE__)

if ARGV.length == 0
  usage
end

configFile = ARGV[0]


def build()


end
